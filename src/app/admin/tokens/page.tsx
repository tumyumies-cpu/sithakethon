
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function TokensPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Tokens</CardTitle>
        <CardDescription>
          This page will allow admins to manage the token economy, issue rewards, and view token analytics.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Content under construction.</p>
      </CardContent>
    </Card>
  );
}
